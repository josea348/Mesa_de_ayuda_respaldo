# Libreias a utilizar

npm install express ejs jsonwebtoken multer dotenv mysql2 express-validator bcrypt cors
npm install nodemon -D

# Para tener en cuenta despues de tener la librerias instaladas

Una vez teniendo el package.json solo instalamos las dependencias:
- npm install

Y corremos el servidor con:
- npm run dev
